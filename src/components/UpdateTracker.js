import React, { Component } from 'react'
import { PropTypes as T } from 'prop-types'

/**
 * First say how long it will be until the next NHC update
 * Then how long since the last
 *
 * Then give a link to seans forcast sheet
 */